# WEB_RESEARCH_BACKLOG — 已查与待查

## 本次已完成的 web research（2026-06-10/11，结果已用于方向研究）

1. forecast tracking / calibration / prediction accountability 市场扫描 ✔
2. AI agent evaluation / LLM observability 市场规模与玩家 ✔
3. Metaculus track record / 评分机制 ✔

## 待查清单（按优先级，附建议搜索式）

### P0 — 影响 14 天内决策
1. **D1 竞品逐家核实**（FP-2 执行时）：
   `Braintrust eval platform pricing` / `Patronus AI evaluation` /
   `Langfuse prediction tracking feature` — 确认无人做预登记收据
2. **agent 采购质询是否真实存在**：
   `enterprise AI agent procurement requirements reliability evidence` /
   `AI vendor due diligence track record questionnaire`
3. **server-side 时间戳的可信方案**：
   `trusted timestamping RFC 3161 free service` / `OpenTimestamps practical guide`

### P1 — 影响 30–60 天
4. forecaster 履历的需求侧证据：
   `substack analyst prediction track record credibility` /
   `fintwit prediction accountability callout`
5. concierge→SaaS 定价参照：
   `AI eval platform pricing per seat 2026` / `audit-as-a-service pricing model`
6. 法务先例：`prediction tracking service investment advice regulation` /
   `forecast publishing platform SEC no-action`（仅作背景，结论仍以 attorney 为准）

### P2 — 背景补全
7. Metaculus/Manifold/Kalshi 的 API 与数据开放度（潜在 resolution 数据源）
8. OSF preregistration 的实际使用量数据（D3 排除决定的复核）
9. golf 数据生态（若 6/13 GO）：`Arccos API third party access` /
   `GHIN handicap API developer`

## 规则

- 每条查完在此打勾并把结论写进对应决策文档（不要堆在这里）
- 搜索结论必须标 [WEB 证实] 或 [推断]，与本地 hypothesis 区分
